# DevDB Pokemon Test Suite
# Install-once test communities themed after the Pokemon world.
# Each community exercises one scenario from the DevDB scenario pack.
#
# Usage:
#   python -m tests.pokemon.install          # install all communities (run once)
#   python -m tests.pokemon.install --reset  # re-install (drops and recreates)
#   python -m tests.pokemon.runner           # run all scenarios
#   python -m tests.pokemon.runner --scenario pallet_town
