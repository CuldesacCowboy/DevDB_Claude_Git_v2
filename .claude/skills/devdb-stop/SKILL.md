---
name: devdb-stop
description: Stop the DevDB backend and frontend servers and close the DevDB Chrome window
---

Run the following command to stop DevDB:

```
cmd /c "C:\DevDB_Claude_Git_v2\Stop_DevDB.bat"
```

Report what happened (stopped cleanly, ports released, etc.).
