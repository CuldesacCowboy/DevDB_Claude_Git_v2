---
name: devdb-start
description: Start the DevDB backend and frontend servers and open Chrome
---

Run the following command to start DevDB:

```
cmd /c "C:\DevDB_Claude_Git_v2\Start_DevDB.bat"
```

Report what happened (started successfully, port already in use, etc.).
