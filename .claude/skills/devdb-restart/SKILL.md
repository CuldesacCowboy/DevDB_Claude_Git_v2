---
name: devdb-restart
description: Restart the DevDB backend and frontend servers (stop, then start fresh)
---

Run the following command to restart DevDB:

```
cmd /c "C:\DevDB_Claude_Git_v2\Restart_DevDB.bat"
```

Report what happened (restarted cleanly, any warnings, etc.).
